package com.microlearn.models;

public class Instructor extends User{

    public Instructor(String fullName, String profilePictureUrl, boolean isVerified, String password, String details) {
        super(fullName, profilePictureUrl, isVerified, password, details);
    }
}
